import * as React from 'react';

interface Props {
  schema: {};
}

/**
 * This is a React Component that will create a dialog to
 * @param schema
 * @constructor
 */
const ReusableFormDialog: React.FC<Props> = ({ schema }) => {
  return <div></div>;
};

export default ReusableFormDialog;
